<%= form_for(tea) do |f| %>
  <% if tea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tea.errors.count, "error") %> prohibited this tea from being saved:</h2>

      <ul>
      <% tea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :caffeine %>
    <%= f.text_field :caffeine %>
  </div>

  <div class="field">
    <%= f.label :times_chosen %>
    <%= f.number_field :times_chosen %>
  </div>

  <div class="field">
    <%= f.label :times_skipped %>
    <%= f.number_field :times_skipped %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
